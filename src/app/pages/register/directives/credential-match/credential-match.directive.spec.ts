import { CredentialMatchDirective } from './credential-match.directive';

describe('CredentialMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new CredentialMatchDirective();
    expect(directive).toBeTruthy();
  });
});
